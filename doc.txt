// Arduinix  совместно с  Skameika
//25.07.2024 версия 2.0.0

описание библеотека rucrypto поддерживает несколько алгоритмов шифрования
в числе которых:
шифр цезаря (Caesar.h)
шифр виженера(Vigenere.h)
RSA(RSA.h)
DES(DES.h)
3DES(3DES.h)
otbash(otbash.h)
бинарный шифр(Binari.h)
xor(xor.h) сущевствовал с версии 1.1.0
в версии 2.0.0 была добавлена возможность работы с файлами а следовательно и новые методы общие для всех шифров они юудут показаны на примере цезаря
Класс для шифрования и дешифрования текста методом Цезаря */ class Caeser { private: std::string str; ///< Хранит введенную пользователем строку int key; ///< Хранит ключ для шифрования и дешифрования

public:
конструктор Caeser::Caeser() инициализирует объект класса Caeser с режимом 0.
конструктор Caeser::Caeser(string filename) инициализирует объект класса Caeser с режимом 1 и открывает файл, указанный в filename для чтения.
конструктор Caeser::Caeser(string inputfile, string outputfile) инициализирует объект класса Caeser с режимом 2, открывает файлы inputfile и outputfile для чтения и записи соответственно.
деструктор Caeser::~Caeser() осуществляет закрытие файловых потоков в зависимости от значения поля mode. Если mode равен 1, то закрывается finputfile. Если mode равен 2, то закрываются finputfile и foutputfile. Если mode равен 3, то закрываются все три потока: finputfile, foutputfile и fkeyfile.


/**
Считывает данные из входного файла на основе режима шифрования и предоставленного ключа.
Если режим равен 3, программа считывает данные из входного файла, шифрует их с помощью шифра Цезаря ключом из файла ключей и записывает результат в выходной файл.
Если значение cryptomode равно 0, данные шифруются.
Если значение cryptomode равно 1, данные расшифровываются.
Если значение mode не равно 3, отображается сообщение об ошибке.

параметр cryptomode - 0 для шифрования, 1 для расшифровки
возвращает 0 в случае успеха, 1 в случае ошибки
 */
int Caeser::read_Datafile(int cryptomode)
/**

Считывает данные из входного файла на основе режима шифрования и предоставленного ключа.
Шифрует или расшифровывает данные на основе режима шифрования и предоставленного ключа.
Если режим равен 1, он считывает данные из входного файла, шифрует/расшифровывает их с помощью шифра Цезаря с предоставленным ключом и записывает результат обратно во входной файл.
Если режим равен 2, программа считывает данные из входного файла, шифрует/расшифровывает их с помощью Caesar cipher с предоставленным ключом и записывает результат в выходной файл.
Если режим равен 3, программа выводит сообщение об ошибке из-за слишком большого количества аргументов.
param cryptomode - 0 для шифрования, 1 для дешифрования
param key - ключ, который будет использоваться для шифрования Caesar
возвращает 0 в случае успеха, 1 в случае ошибки */ 
int Caeser::read_Datafile(int cryptomode, int key)
/**
*  Устанавливает значение строки для шифрования и дешифрования
* \param str - строка для шифрования и дешифрования
* \return строка для работы с методами шифрования и дешифрования
*/
std::string setword(std::string str);вболее ранних версиях называлась getword (хотя являлась сеттером)


/**
*  Проверяет ключ на допустимость
* \param key - ключ для шифрования и дешифрования
* \return 0 - если ключ верный, 1 - если ключ равен 0, -1 - если ключ не является целым числом
*/
int chekkey(int key);

/**
* \brief Устанавливает значение ключа для шифрования и дешифрования
* \param key - ключ для шифрования и дешифрования
* \return ключ для работы с методами шифрования и дешифрования
*/
int setkey(int key);в более ранних версиях называлась getkey (хотя являлась сеттером)

/**
*  Шифрует строку по методу Цезаря
* string str - строка для шифрования
* int  key - ключ для шифрования
* \return зашифрованную строку
*/
std::string encript(std::string str, int key);

/**
*  Дешифрует строку, зашифрованную методом Цезаря
* string str - строка для дешифрования
* int key - ключ для дешифрования
* \return расшифрованную строку
*/
std::string decript(std::string str, int key);
};

**/
// документация на DES(DES.h)
 для работы с файлами
DES();
 DES(string inputfile);
 DES(string inputfile, string outputfile);
 DES(string inputfile, string outputfile, string keyfile);
 ~DES();
 int read_Datafile(int cryptomode, string key);
 int read_Datafile(int cryptomode);

// Определение константных массивов для алгоритма DES
const int IP[64] = { /* ... */ };
const int IP_1[64] = { /* ... */ };
const int E_exp[48] = { /* ... */ };
const int P_transform[32] = { /* ... */ };
const int PC_1[56] = { /* ... */ };
const int PC_2[48] = { /* ... */ };
const int SBOX_1[4][16] = { /* ... */ };
// ...
const int SBOX_8[4][16] = { /* ... */ };
SBOX SBox[8] = { SBOX_1, SBOX_2, SBOX_3, SBOX_4, SBOX_5, SBOX_6, SBOX_7, SBOX_8 };

// Основной класс DES с методами для шифрования и дешифрования
class DES {
    // Члены класса для хранения ключей, кодов и промежуточных данных
    // ...

    // Метод для кодирования строки с использованием ключа
    void encode(string str, string key) {
        // ...
    }

    // Метод для декодирования строки с использованием ключа
    void decode(string str, string key) {
        // ...
    }

    // Вспомогательные методы для реализации алгоритма DES
    void fill(string str) { /* ... */ }
    void formatSourceCode() { /* ... */ }
    void getIP0() { /* ... */ }
    string Feistel(string R, string K) { /* ... */ }
    string getPTransform(string str) { /* ... */ }
    string Feistel_SBOX(string str, int num) { /* ... */ }
    string E_expend(string str) { /* ... */ }
    string XORoperation(string a, string b) { /* ... */ }
    string iterationT_A(int begin, int end) { /* ... */ }
    string iterationT_D(int begin, int end) { /* ... */ }
    string getIP1(string str) { /* ... */ }
    string formatResult(string str) { /* ... */ }
    string formatAndReduceResult(string str) { /* ... */ }
    int Two2Ten(string num) { /* ... */ }
    string formatKey() { /* ... */ }
    string getPC1Key(string str) { /* ... */ }
    void get_C_D(string str) { /* ... */ }
    void getKeyI() { /* ... */ }
    void LS_1(string& str) { /* ... */ }
    void LS_2(string& str) { /* ... */ }
    string getPC2Key(string str) { /* ... */ }
    void getKeys() { /* ... */ }
    int chekkey(string key) { /* ... */ }
};
докуменгтация на RSA(RSA.h)
для работы с файлами
RSA();
RSA(string inputfile);
RSA(string inputfile, string outputfile);
RSA(string inputfile, string outputfile, string keyfile);
~RSA();
int read_Datafile(int cryptomode, int p,int q);
int read_Datafile(int cryptomode);


// Статические члены класса RSA, используемые для шифрования и дешифрования
long RSA::e; // Открытая экспонента
long RSA::n; // Модуль
long RSA::d; // Секретная экспонента

// Метод для шифрования сообщения
string RSA::encrypt(string msg)
{
    // ...
}

// Метод для дешифрования сообщения
string RSA::decrypt(string msg)
{
    // ...
}

// Метод для генерации ключей RSA
int RSA::genereytkey(int p, int q)
{
    // ...
}

// Вспомогательные методы для реализации алгоритма RSA
bool RSA::isPrime(long int prime) { /* ... */ }
long int RSA::greatestCommonDivisor(long int e, long int t) { /* ... */ }
long int RSA::calculateE(long int t) { /* ... */ }
long int RSA::calculateD(long int e, long int t) { /* ... */ }
long int RSA::encryptions(long int i, long int e, long int n) { /* ... */ }
long int RSA::decryptions(long int i, long int d, long int n) { /* ... */ }


// Статические члены класса RSA, используемые для шифрования и дешифрования
long RSA::e; // Открытая экспонента
long RSA::n; // Модуль
long RSA::d; // Секретная экспонента

// Метод для шифрования сообщения
string RSA::encrypt(string msg)
{
    // ...
}

// Метод для дешифрования сообщения
string RSA::decrypt(string msg)
{
    // ...
}

// Метод для генерации ключей RSA
int RSA::genereytkey(int p, int q)
{
    // ...
}

// Вспомогательные методы для реализации алгоритма RSA
bool RSA::isPrime(long int prime) { /* ... */ }
long int RSA::greatestCommonDivisor(long int e, long int t) { /* ... */ }
long int RSA::calculateE(long int t) { /* ... */ }
long int RSA::calculateD(long int e, long int t) { /* ... */ }
long int RSA::encryptions(long int i, long int e, long int n) { /* ... */ }
long int RSA::decryptions(long int i, long int d, long int n) { /* ... */ }

Этот код реализует алгоритм шифрования RSA. Он содержит методы для шифрования и дешифрования текста, а также для генерации ключей RSA. 
Ключевые методы включают проверку на простоту числа, вычисление наибольшего общего делителя, открытой и секретной экспонент,
а также функции шифрования и дешифрования.
Алгоритм RSA использует два ключа — открытый и закрытый. Открытый ключ состоит из числа 
n (произведение двух простых чисел p и q) и открытой экспоненты e. 
Закрытый ключ состоит из того же числа n и секретной экспоненты d. 
Шифрование и дешифрование выполняются с использованием модульных арифметических операций.


//докуменгтация на шифр виженера(Vigenere.h)
новые методы
VigenereCipher();
VigenereCipher(string inputfile);
VigenereCipher(string inputfile, string outputfile);
VigenereCipher(string inputfile, string outputfile, string keyfile);
~VigenereCipher();
int read_Datafile(int cryptomode, string key);
int read_Datafile(int cryptomode);


// Класс VigenereCipher с методами для установки ключа, шифрования и дешифрования
class VigenereCipher {
    string key; // Ключ шифрования

    // Метод для установки ключа шифрования
    void setkey(string key);

    // Метод для шифрования текста
    string encrypt(const string& text);

    // Метод для дешифрования текста
    string decrypt(const string& text);

    // Вспомогательные методы
    string getKey(const string& text, const string& key);
    char cipherChar(char c, char key, bool encode);
};
Этот код реализует алгоритм шифрования Виженера. Он содержит методы для установки ключа, шифрования и дешифрования текста. Ключевые методы включают в себя получение эффективного ключа и шифрование/дешифрование каждого символа текста.

Основные методы:

setkey(string key): Преобразует ключ в верхний регистр и сохраняет его в члене класса key.
encrypt(const string& text): Шифрует текст, используя эффективный ключ, полученный из метода getKey.
decrypt(const string& text): Дешифрует текст, используя эффективный ключ, полученный из метода getKey.
Вспомогательные методы:

getKey(const string& text, const string& key): Генерирует эффективный ключ, повторяя исходный ключ до длины текста.
cipherChar(char c, char key, bool encode): Шифрует или дешифрует отдельный символ, используя заданный ключ.

// документация на otbash(otbash.h)

#################### Описание ####################

Файл otbash.h содержит объявление класса Otbash, который предствляет собой реализацию шифра Атбаш на языке C++


#################### Основная теория ####################

Шифр Атбаш использует входной параметр в виде символов


#################### Инициализация ####################

#include "otbash.h"
Otbash atbash; // Создание объекта


#################### Список функций ####################

новые методы
Otbash();
Otbash(string inputfile);
Otbash(string inputfile, string outputfile);
~Otbash();
int read_Datafile(int cryptomode);

1.1 Файл "otbash.h" содержит объявление класса Otbash. Публичные методы:
1.1.1 string encryptOtbas(string text); // Метод для шифрования текста. Принимает на вход строку text и возвращает зашифрованную строку
1.1.2 string decryptOtbas(string text); // Метод для дешифрования текста. Принимает на вход строку text и возвращает дешифрованную строку

1.2 Файл "otbash.h" содержит объявление класса Otbash. Приватные переменные:
1.2.1 string text; // Переменная, содержащая строку, которая является текстом для шифрования и дешифрования

2.1 Файл "otbash.cpp" содержит реализацию методов класса Otbash, описанного в файле "otbash.h". Методы:
2.1.1 encryptOtbas(string text): реализация метода шифрования текста. Осуществляет шифрование строки text с помощью шифра Атбаш
2.1.2 decryptOtbas(string text): реализация метода дешифрования текста. Осуществляет дешифрование строки text с помощью шифра Атбаш


#################### Примеры ####################

1. testotbash.cpp - пример шифрования слова apple
#################### Описание ####################

Файл 3DES.h содержит объявление класса threDES, который предствляет собой реализацию шифра 3DES на языке C++


#################### Основная теория ####################

Шифр 3DES использует входной параметр в виде символов в качестве строки для шифрования и 3 входных параметра в качестве ключей


#################### Список функций ####################

1.1 Файл "3DES.h" содержит объявление класса threDES. Публичные методы:

новые методы
threDES();
threDES(string inputfile);
threDES(string inputfile, string outputfile);
threDES(string inputfile, string outputfile, string keyfile);
~threDES();
int read_Datafile(int cryptomode, string k1,string k2, string k3);
int read_Datafile(int cryptomode);


1.1.1 string encrypt(string k1, string k2, string k3, string word); // Метод для шифрования текста. Принимает на вход 3 строки k1, k2, k3, строку text и возвращает зашифрованную строку
1.1.2 string decrypt(string k1, string k2, string k3, string word); // Метод для шифрования текста. Принимает на вход 3 строки k1, k2, k3, строку text и возвращает дешифрованную строку

1.2 Файл "3DES.h" содержит объявление класса threDES. Приватные переменные:
1.2.1 string k1; // Переменная, содержащая строку, которая является ключом для шифрования и дешифрования
1.2.2 string k2; // Переменная, содержащая строку, которая является ключом для шифрования и дешифрования
1.2.3 string word = ""; // Переменная, содержащая строку, которая является текстом для шифрования и дешифрования
1.2.4 string k3; // Переменная, содержащая строку, которая является ключом для шифрования и дешифрования
1.2.5 string encode_str = ""; // Переменная, содержащая строку, которая является промежуточным текстом при шифровании
1.2.6 string decode_str = ""; // Переменная, содержащая строку, которая является промежуточным текстом при дешифровании
1.2.7 string str; // Переменная, содержащая строку

2.1 Файл "3DES.cpp" содержит реализацию методов класса threDES, описанного в файле "3DES.h". Методы:
2.1.1 encrypt(string k1, string k2, string k3, string word): реализация метода шифрования текста. Осуществляет шифрование строки word с помощью шифра 3DES
2.1.2 decrypt(string k1, string k2, string k3, string word): реализация метода дешифрования текста. Осуществляет дешифрование строки word с помощью шифра 3DES

#################### Описание ####################

Файл Binari.h содержит объявление класса Binari, который предствляет собой реализацию бинарного шифра на языке C++


#################### Основная теория ####################

Шифр Binari использует входной параметр в виде символов в качестве строки для шифрования


#################### Список функций ####################

новые функции
Binari();
Binari(string inputfile);
Binari(string inputfile, string outputfile);
Binari(string inputfile, string outputfile, string keyfile);
~Binari();
int read_Datafile(int cryptomode, int key);
int read_Datafile(int cryptomode);

1.1 Файл "Binari.h" содержит объявление класса Binari. Публичные методы:
1.1.1 string encrypt(int key,string text);// Метод для шифрования текста. Принимает на вход строку text и ключь key в виде числа представляющий разрядность
 от 2 до 64  и возвращает зашифрованную строку 
1.1.2 string decrypt(string Text); // Метод для шифрования текста. Принимает на вход строку text и ключь key в виде числа представляющий разрядность
 от 2 до 64  и возвращает дешифрованную строку

1.2 Файл "Binari.h" содержит объявление класса Binari. Приватные переменные:
1.2.1 string text=""; // Переменная, содержащая строку, которая является текстом для шифрования и дешифрования

2.1 Файл "3DES.cpp" содержит реализацию методов класса threDES, описанного в файле "3DES.h". Методы:
2.1.1 encrypt(string text): реализация метода шифрования текста. Осуществляет шифрование строки word с помощью бинарного шифра
2.1.2 decrypt(string text): реализация метода дешифрования текста. Осуществляет дешифрование строки word с помощью бинарного шифра

xor(xor.h)
XOR();
XOR(string inputfile);
XOR(string inputfile, string outputfile);
XOR(string inputfile, string outputfile, string keyfile);
~XOR();
int read_Datafile(int cryptomode, string key);
int read_Datafile(int cryptomode);
/**
Расшифровывает заданный текст, который был зашифрован с помощью XOR-шифрования с указанным ключом.

param text Зашифрованный текст, который нужно расшифровать

param key - ключ, используемый для шифрования
/*
string decrypt(string text, string key);
/**

Шифрует заданный текст, используя XOR-шифрование с указанным ключом.

@param text - текст, который нужно зашифровать

@param key - ключ, который нужно использовать для шифрования

@return - encrypted_text
*/
string encrypt(string text, string key);


